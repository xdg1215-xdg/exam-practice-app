name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        if: success()
        run: npm install --legacy-peer-deps

      - name: Build web app
        if: success()
        run: npm run build

      - name: Setup Java
        if: success()
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Download Android SDK
        if: success()
        run: |
          mkdir -p $HOME/android-sdk/cmdline-tools
          cd $HOME/android-sdk/cmdline-tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdtools.zip
          unzip -q cmdtools.zip
          mv cmdline-tools latest
          rm cmdtools.zip
          echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$HOME/android-sdk" >> $GITHUB_ENV
          echo "PATH=$HOME/android-sdk/cmdline-tools/latest/bin:$HOME/android-sdk/platform-tools:$PATH" >> $GITHUB_ENV

      - name: Install Android SDK components
        if: success()
        run: |
          yes | sdkmanager --licenses
          sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - name: Add Capacitor Android
        if: success()
        run: |
          rm -rf android
          npx cap add android

      - name: Sync Capacitor
        if: success()
        run: npx cap sync android

      - name: Create local.properties
        if: success()
        run: |
          mkdir -p android
          echo "sdk.dir=$HOME/android-sdk" > android/local.properties
          echo "flutter.sdk=/dev/null" >> android/local.properties || true

      - name: Install launcher icon (multi-density)
        if: success()
        run: |
          set -e
          RES=android/app/src/main/res
          mkdir -p $RES/mipmap-mdpi $RES/mipmap-hdpi $RES/mipmap-xhdpi $RES/mipmap-xxhdpi $RES/mipmap-xxxhdpi
          sips -z 48 48   src/assets/logo.png --out $RES/mipmap-mdpi/ic_launcher.png    > /dev/null
          sips -z 72 72   src/assets/logo.png --out $RES/mipmap-hdpi/ic_launcher.png    > /dev/null
          sips -z 96 96   src/assets/logo.png --out $RES/mipmap-xhdpi/ic_launcher.png   > /dev/null
          sips -z 144 144 src/assets/logo.png --out $RES/mipmap-xxhdpi/ic_launcher.png  > /dev/null
          sips -z 192 192 src/assets/logo.png --out $RES/mipmap-xxxhdpi/ic_launcher.png > /dev/null
          echo "Launcher icons installed (48/72/96/144/192 px)"

      - name: Build APK
        if: success()
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
